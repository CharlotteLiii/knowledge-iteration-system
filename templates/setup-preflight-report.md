# 四层知识系统检查结果

## 1. 知识库路径

- 根路径：{{knowledge_base_root}}
- 是否存在：{{是 / 否}}
- 是否可读：{{是 / 否 / 未验证}}
- 是否可写：{{是 / 否 / 未验证}}
- 配置文件：{{已找到 / 未找到 / 需要创建}}

## 2. 四层映射

| 层级 | 状态 | 路径 | 备注 |
|---|---|---|---|
| 输入层 Inbox | {{已找到 / 待确认 / 缺失}} | {{path}} | {{note}} |
| 蒸馏层 Distilled | {{已找到 / 待确认 / 缺失}} | {{path}} | {{note}} |
| 技能层 Skills | {{已找到 / 待确认 / 缺失}} | {{path}} | {{note}} |
| 输出层 Output | {{已找到 / 待确认 / 缺失}} | {{path}} | {{note}} |

## 3. 子文件夹检查

| 子文件夹角色 | 状态 | 路径 |
|---|---|---|
| 想法/灵感集 | {{已找到 / 缺失 / 不适用}} | {{path}} |
| Clippings | {{已找到 / 缺失 / 不适用}} | {{path}} |
| 每日蒸馏 | {{已找到 / 缺失 / 不适用}} | {{path}} |
| 每周复盘 | {{已找到 / 缺失 / 不适用}} | {{path}} |
| 想法追踪 | {{已找到 / 缺失 / 不适用}} | {{path}} |
| Clippings提炼 | {{已找到 / 缺失 / 不适用}} | {{path}} |

## 4. 检查结论

当前状态：{{完整标准结构 / 无四层结构 / 已有类似结构需映射 / 部分缺失 / 路径不可访问}}

说明：
{{summary}}

## 5. 建议动作

- {{无需操作 / 需要用户确认四层映射 / 可创建缺失文件夹 / 需要用户提供路径 / 需要用户授权写入}}
- {{action_2}}
- {{action_3}}

## 6. 将要创建的文件夹（如适用）

以下操作只创建缺失文件夹，不覆盖、不删除、不移动已有文件：

```text
{{folders_to_create}}
```

## 7. 用户确认项

请确认：

1. 知识库根目录是否正确：{{knowledge_base_root}}
2. 四层映射是否正确：{{是 / 否}}
3. 是否允许创建缺失文件夹：{{是 / 否}}
4. 是否允许写入 `.knowledge-iteration-system.json`：{{是 / 否}}

## 8. 本地配置预览

```json
{{config_preview}}
```
