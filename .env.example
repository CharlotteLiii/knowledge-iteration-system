# Knowledge Iteration System - LLM 配置示例
# 用法：把这个文件复制成 `.env` 放到你的 Vault 根路径，然后按下面说明填。
# `.gitignore` 已经把真实的 `.env` 排除了，安全。
#
# ============================================================
# 【必读】你需要向 LLM 供应商拿到以下 3 项信息
# ============================================================
#
# 1. KIS_LLM_BASE_URL
#    OpenAI 兼容的 API 根地址（一般以 /v1、/v3、/paas/v4 结尾）
#    示例：https://api.openai.com/v1
#
# 2. KIS_LLM_MODEL
#    要调用的模型名，必须是该供应商列表里的一个
#    示例：gpt-4o-mini / deepseek-chat / glm-4.5-flash / ark-code-latest
#
# 3. KIS_LLM_API_KEY
#    该供应商颁发的 API Key（一般以 sk- / ark- 等前缀开头）
#
# 要求：供应商必须提供 OpenAI 兼容的 /chat/completions 接口。
# 大多数国内外主流 LLM 都兼容（OpenAI / Anthropic 中转 / 智谱 / DeepSeek / 通义 / 火山方舟 等）。
#
# ============================================================
# 【选填】兼容开关
# ============================================================
#
# KIS_LLM_DISABLE_JSON_MODE=1
#   如果供应商不支持 response_format={"type":"json_object"}
#   （报 InvalidParameter/BadRequest 或类似错误），加这行关闭强制 JSON 模式。
#   已知需要关闭的：火山方舟 Ark plan endpoint 的 ark-code-latest。
#   prompt 里已明确要求 JSON，脚本仍能兼容纯文本响应。
#
# ============================================================
# 在下面填你自己的配置（把示例块取消注释，改成你的真实值）
# ============================================================

# ---- 你的配置 ----
# KIS_LLM_BASE_URL=
# KIS_LLM_MODEL=
# KIS_LLM_API_KEY=

# ---- 参考示例（任选一个供应商，改成你自己的 key）----

# OpenAI
# KIS_LLM_BASE_URL=https://api.openai.com/v1
# KIS_LLM_MODEL=gpt-4o-mini
# KIS_LLM_API_KEY=sk-...

# DeepSeek
# KIS_LLM_BASE_URL=https://api.deepseek.com
# KIS_LLM_MODEL=deepseek-chat
# KIS_LLM_API_KEY=sk-...

# 智谱 GLM
# KIS_LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# KIS_LLM_MODEL=glm-4.5-flash
# KIS_LLM_API_KEY=...

# 火山方舟 Ark（plan endpoint 需要加 KIS_LLM_DISABLE_JSON_MODE=1）
# KIS_LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/plan/v3
# KIS_LLM_MODEL=ark-code-latest
# KIS_LLM_API_KEY=ark-...
# KIS_LLM_DISABLE_JSON_MODE=1

# ==== 说明 ====
# - 不配置 API key 时脚本自动降级为纯正则模式，仍能跑；LLM 兜底会静默跳过
# - 想验证配置：`python3 scripts/kis_llm.py`（会打印 READY / DEGRADED 状态）
# - 参考：references/config-schema.md
